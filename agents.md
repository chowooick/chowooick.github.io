# AI 코딩 에이전트를 병렬로 굴릴 때의 사실

한 리포지토리에 에이전트 여러 개가 각자 티켓 하나씩을 맡아 동시에 작업하는 방식에서
실제로 겪은 것. 프로젝트 무관하게 적용된다.

## 코드보다 먼저 명세의 결함을 말하게 하라

규칙 한 줄:

> 티켓을 읽은 직후, 코드를 건드리기 전에 티켓의 결함을 최대 3개 보고한다(없으면 "없음").
> 보고 후 바로 수행에 들어간다. 답을 기다리지 않는다.

티켓 12개에서 결함 12건이 나왔고 **10건이 명세를 쓴 쪽의 오류**였다. 코드 버그가 아니라
요구사항의 구멍이다.

실제로 잡힌 것:
- 존재하지 않는 브랜치를 쓰라고 지정
- 성립할 수 없는 버전 규칙 (`1.x` 라인을 "서버와 같은 마이너"로 요구)
- 서로 모순되는 완료 기준 두 개 (같은 출력 줄에 상반된 정규식)
- 필요한 기능을 막는 금지 조항 (초기 스파이크용 제약을 후속 티켓에 그대로 복사)
- 물리적으로 구현 불가능한 완료 기준 (프레임워크가 그 필드를 와이어에 싣지 않음)
- 하드코딩된 테스트 포트 — 동시에 한 티켓만 통합 테스트 가능

## 별도 리뷰어 에이전트는 구조적으로 실패한다

"티켓을 발급 전에 검토하는 에이전트"를 따로 두는 방식은 작동하지 않았다.
리뷰어는 **명세를 쓴 쪽이 준 지시문**을 받는다. 지시자가 놓친 것은 지시문에도 없으므로
리뷰어도 놓친다. 독립적 검증이 아니라 같은 맹점에 단계만 추가한 것이다.

수행하는 에이전트는 다르다. 나쁜 티켓이면 자기가 3회 실패하고 정지한다 —
인센티브가 정렬돼 있고, 왜 그 줄을 썼는지 모르는 채로 읽는다.

## 보고하고 대기하지 마라

에이전트가 답을 기다리면 모든 결함이 사람을 통과하는 왕복이 되고, 없애려던 병목이 그대로
돌아온다. 허공에 보고하고 자기 판단으로 진행하는 편이 낫다 —
보고를 읽으면서 잘못 간 것만 중간에 잡으면 된다.

티켓 12개에서 에이전트의 자체 처리를 뒤집은 것은 **2건**이었다.

## 계약 문서를 먼저 확정하라

서버 API를 여러 티켓이 나눠 만들면, 각 티켓이 같은 계약 문서를 고치려다 충돌한다.

- 계약(요청·응답 형태, 오류 식별자)을 **코드보다 먼저** 한 사람이 쓴다
- 티켓은 계약을 **읽기만** 한다. 바꿔야 하면 정지하고 보고
- 계약이 원본이고 코드가 따라간다

계약에 거짓이 들어가면 그걸 믿고 짠 클라이언트가 깨진다. 실측으로 확인된 것만 적을 것.

## 제약은 품질 상한이 된다

일정을 지키려고 건 제약이 결과물 품질의 천장이 된다.

사례: 3D 홍보 컷 제작에 "내장 프리미티브 + 단색 머티리얼만"을 걸었다. 모델링에 시간이
빨려 들어가는 것을 막으려는 의도였고, 그 목적으로는 맞았다. 결과물은 "그림판으로 그린
아마추어" 평가를 받았다.

3D 스틸의 품질은 지오메트리가 아니라 **조명·머티리얼·후처리·구도**에서 나온다.
넷을 전부 막고 가장 덜 중요한 것만 남겨 뒀다.

수정: 형상은 코드 생성이면 자유(프리미티브·CSG·ArrayMesh), 파일 임포트만 금지(라이선스),
조명·머티리얼·후처리는 제한 없음. **일정을 지키는 것은 파일 임포트 금지 쪽이지 형상 제한이 아니었다.**

같은 패턴이 반복됐다: "오토로드 금지"는 스파이크의 과설계를 막았지만 나중에 씬마다
재인증을 강제했다. "로컬 툴체인 금지"는 빌드 재현성을 지켰지만 DB가 필요한 단위 테스트를
불가능하게 만들었다.

**결과물이 특정 방향으로 나쁘면 실행을 탓하기 전에 자기 제약을 읽어라.**

## 에이전트가 자기 결과물을 보게 하라

이미지·렌더 산출물은 에이전트가 읽기 도구로 직접 열어 볼 수 있다.
한 번 만들고 끝내는 것과, 만들고 → 보고 → 채점하고 → 고치는 루프를 4회 도는 것은
결과가 다르다.

채점 항목은 보면 답할 수 있는 형태로 쓴다:
- 그림자가 소프트한가
- 그림자 안이 검정인가, 하늘색으로 물들어 있는가
- 물체마다 roughness가 다른가
- 주제가 정중앙에 박혀 있지 않은가

## 병렬 작업의 실무 규칙

**워크트리를 만들지 않는다.** 격리는 touch list(티켓별로 건드릴 파일 목록)로 한다.
워크트리는 머지 부담과 경로 혼선만 늘린다.

**`git add -A`를 쓰지 않는다.** 병렬 세션이 같은 워킹 트리를 공유하므로 남의 미완성
파일이 딸려 들어간다. 실제로 문서 커밋 하나에 다른 세션의 작업 파일 60개가 섞여 들어갔다.
항상 경로를 명시한다.

**커밋은 한 곳에서만 한다.** 수행 에이전트는 커밋하지 않는다. 검토한 쪽이 경로를 지정해
커밋한다.

**테스트 스택은 슬롯으로 나눈다.** 포트를 하드코딩하면 동시에 한 티켓만 통합 테스트를
돌릴 수 있다. 빈 슬롯을 자동으로 잡는 방식이 필요하다.

## 조정자 역할은 지정되어야 한다

세션이 재시작되면 이름이 유실될 수 있다. 그러면 작업 에이전트들이 조정자를 찾지 못하고,
다른 세션이 "내가 조정자다"라고 나서면서 지시가 두 갈래로 갈린다.
실제로 같은 작업 에이전트가 상반된 결정을 두 주소에서 받았다.

- 조정자는 사람이 지정한다. 어떤 에이전트도 자처하지 않는다
- 기준은 이름이 아니라 **티켓을 준 주소**다
- 그 주소가 사라지면 정지하고 대기한다

## 백그라운드 에이전트는 부모와 함께 죽는다

`--bg`로 띄운 작업 세션은 조정자 세션의 자식이다. 조정자가 재시작되면 전부 죽는다.
**작업물은 워킹 트리에 남으므로 손실은 없다.**

재시작 후에는 `git status`와 "worklog 파일이 있는가"로 중단된 티켓을 찾아,
디스크에 이미 있는 것을 알려주며 **재개**시킨다. 처음부터 다시 시키지 말 것.

## 완료 보고 형식을 고정하라

작업 세션이 끝날 때 남기는 기록을 형식으로 못박으면 검토가 기계적으로 된다:

1. 한 것 / 2. 안 한 것 / 3. 막힌 것 / 4. **작업 범위 밖 변경**(없으면 "없음", 있으면 경로와 이유) /
5. **건드린 절대 규칙과 지킨 방법** / 6. 조정자 결정 필요 / 7. 부산물 여부

4번이 핵심이다. 에이전트가 스스로 신고하고, 조정자가 뒤에서 `git status`로 대조한다.
**둘이 어긋나면 그 자체가 신호다.**
